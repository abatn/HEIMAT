# HEIMAT – Fortschritt

## ARCHITEKTUR-ANALYSE (aus MD-Dateien + Fehleranalyse)

### Daten-Pipeline
```
PostgreSQL (DECIMAL) → Node.js pg (String) → JSON (String) → Flutter (.toDouble() scheitert)
```

### Ursache
- PostgreSQL: `latitude DECIMAL(10, 8)` gibt Strings zurück
- Node.js pg: Gibt DECIMAL-Werte als Strings in JSON
- Flutter: Erwartet Numbers, bekommt Strings

### Korrekte Lösung
**Nicht** nur `.toDouble()` fixen, sondern:
1. **Backend:** Daten-Typen in PostgreSQL korrekt definieren
2. **Oder:** Frontend: Strings robust parsen mit `double.parse()`
3. **Oder:** API: Daten im Backend konvertieren

### Empfehlung
**Option 2 (Frontend-Fix)** ist am einfachsten:
- `double.parse(json['latitude'].toString())` statt `(json['latitude'] ?? 0).toDouble()`
- Das ist der korrekte Weg weil PostgreSQL DECIMAL immer als String kommt

## FEATURES (aus README.md)
- Mobilität: Karte + ÖPNV + Routing
- Finanzen: P2P-Zahlungen (Taler)
- Gesundheit: Arzt-Suche + Terminbuchung

## TEST-CHECKLISTE (aus docs/endbenutzer-testing.md)
- [ ] Karte lädt korrekt
- [ ] ÖPNV-Suche funktioniert
- [ ] Guthaben wird angezeigt
- [ ] Zahlung funktioniert
- [ ] Ärzte werden angezeigt
- [ ] Terminbuchung funktioniert

## NÄCHSTE PHASE: Data-Flow korrigieren

### Schritte
1. `mobility_provider.dart`: `double.parse()` statt `.toDouble()`
2. `finance_provider.dart`: Gleiches Problem prüfen
3. `health_provider.dart`: Gleiches Problem prüfen
4. App testen
5. Features einzeln prüfen

### Verifikation
- Alle 6 Features funktionieren
- Keine Typ-Fehler mehr

## AI-STRATEGIE (aus AI-*.md)

### Kernprinzipien (AI-Strategy.md)
- 100% Open Source (TensorFlow Lite, Vosk, spaCy, Code Llama)
- On-Device AI bevorzugt (keine Daten in der Cloud)
- DSGVO-konform (Art. 5, 7, 17)
- Kosten: <€100/Jahr

### Architektur (AI-Architektur.md)
- Frontend: TensorFlow Lite, Vosk, Coqui TTS
- Backend: MLflow, FastAPI, Redis
- ML-Modelle: LightGBM, spaCy, Code Llama, Surprise

### Implementierungsplan (AI-Implementierungsplan.md)
- Phase 1 (Monat 6-9): On-Device AI
- Phase 2 (Monat 9-12): Cloud AI
- Phase 3 (Monat 12-15): Generative AI
- Phase 4 (Monat 15-18): Vollintegration

### Rechtliches (AI-Rechtliches.md)
- DSGVO-konform: On-Device AI, Opt-in, Löschkonzept
- Keine BaFin-Lizenz, keine TI-Anbindung

### Reihenfolge
1. **Erst:** App muss funktionieren (Data-Flow-Fix)
2. **Dann:** Features laut README.md
3. **Dann:** AI-Integration (Monat 6-18)

## STATUS
- 🟢 CI/CD: 10/10 grün
- 🟢 BACKEND: Port 3000
- 🔴 APP: Data-Flow-Fehler (DECIMAL → String → .toDouble())
