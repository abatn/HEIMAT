# HEIMAT – Fortschritt

## PROJEKT
HEIMAT – Die deutsche Super App für Lebensvereinfachung

## LETZTE AKTION
- Korrekter Implementierungsplan erstellt

## NÄCHSTE AKTION
- Schritt 1: PostgreSQL-Schema

---

## IMPLEMENTIERUNGSPLAN – 100% ECHTER CODE

### Aktueller Stand (Dateien)
```
src/backend/
├── src/
│   ├── config/database.ts      (DB-Verbindung - LEER)
│   ├── services/
│   │   ├── mobilityService.ts   (MOCK-Daten)
│   │   ├── financeService.ts    (MOCK-Daten)
│   │   └── healthService.ts     (MOCK-Daten)
│   ├── routes/
│   │   ├── mobility.ts
│   │   ├── finance.ts
│   │   └── healthService.ts
│   ├── middleware/
│   ├── utils/logger.ts
│   └── index.ts
├── package.json
├── tsconfig.json
└── .eslintrc.json

src/mobile/lib/
├── main.dart
├── core/
│   ├── config/app_config.dart
│   └── theme/app_theme.dart
└── features/
    ├── mobility/presentation/mobility_screen.dart (MOCK)
    ├── finance/presentation/finance_screen.dart (MOCK)
    └── health/presentation/health_screen.dart (MOCK)
```

### Was fehlt (echter Code)
1. **PostgreSQL-Schema** – Keine Tabellen
2. **DB-Config** – database.ts ist leer
3. **Services** – Nur Mock-Daten, keine DB
4. **Frontend API** – Keine HTTP-Calls
5. **Docker** – Kein docker-compose.yml

---

### Schritt 1: PostgreSQL-Schema
**Datei:** `src/backend/src/database/schema.sql`

**Tabellen:**
```sql
-- doctors
CREATE TABLE doctors (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name VARCHAR(255) NOT NULL,
  specialty VARCHAR(100) NOT NULL,
  address TEXT NOT NULL,
  phone VARCHAR(50),
  email VARCHAR(255),
  latitude DECIMAL(10,8),
  longitude DECIMAL(11,8),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- doctor_slots
CREATE TABLE doctor_slots (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  doctor_id UUID REFERENCES doctors(id) ON DELETE CASCADE,
  day_of_week INTEGER NOT NULL,  -- 0-6
  start_time TIME NOT NULL,
  end_time TIME NOT NULL,
  is_available BOOLEAN DEFAULT true
);

-- appointments
CREATE TABLE appointments (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  doctor_id UUID REFERENCES doctors(id),
  patient_name VARCHAR(255) NOT NULL,
  patient_email VARCHAR(255),
  appointment_date DATE NOT NULL,
  appointment_time TIME NOT NULL,
  status VARCHAR(20) DEFAULT 'pending',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- wallets
CREATE TABLE wallets (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id VARCHAR(255) NOT NULL UNIQUE,
  balance DECIMAL(10,2) DEFAULT 0.00,
  currency VARCHAR(3) DEFAULT 'EUR',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- transactions
CREATE TABLE transactions (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  from_wallet_id UUID REFERENCES wallets(id),
  to_wallet_id UUID REFERENCES wallets(id),
  amount DECIMAL(10,2) NOT NULL,
  currency VARCHAR(3) DEFAULT 'EUR',
  status VARCHAR(20) DEFAULT 'completed',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- stops (Haltestellen)
CREATE TABLE stops (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name VARCHAR(255) NOT NULL,
  latitude DECIMAL(10,8) NOT NULL,
  longitude DECIMAL(11,8) NOT NULL,
  stop_type VARCHAR(50)
);
```

**Seed Data:** Echte Daten (Berlin, München, Hamburg)

---

### Schritt 2: Backend Database-Config
**Datei:** `src/backend/src/config/database.ts`

```typescript
import { Pool } from 'pg';

const pool = new Pool({
  host: process.env.DB_HOST || 'localhost',
  port: parseInt(process.env.DB_PORT || '5432'),
  database: process.env.DB_NAME || 'heimat',
  user: process.env.DB_USER || 'postgres',
  password: process.env.DB_PASSWORD || 'postgres',
});

export async function query(text: string, params?: any[]) {
  const result = await pool.query(text, params);
  return result.rows;
}
```

---

### Schritt 3: Backend Services (mit echter DB)
**Alle Services umschreiben:**
- `mobilityService.ts` → DB-Abfragen statt Mock
- `financeService.ts` → DB-Abfragen statt Mock
- `healthService.ts` → DB-Abfragen statt Mock

**Beispiel mobilityService.ts:**
```typescript
import { query } from '../config/database';

export async function getStops(lat: number, lng: number, radius: number) {
  return query(
    `SELECT * FROM stops 
     WHERE ST_Distance(
       ST_SetSRID(ST_MakePoint(longitude, latitude), 4326)::geography,
       ST_SetSRID(ST_MakePoint($1, $2), 4326)::geography
     ) < $3`,
    [lng, lat, radius]
  );
}
```

---

### Schritt 4: Frontend API-Client
**Datei:** `src/mobile/lib/core/api/api_client.dart`

```dart
import 'package:http/http.dart' as http;
import 'dart:convert';

class ApiClient {
  final String baseUrl = 'http://10.0.2.2:3000'; // Android Emulator
  
  Future<Map<String, dynamic>> get(String endpoint) async {
    final response = await http.get(Uri.parse('$baseUrl$endpoint'));
    return json.decode(response.body);
  }
  
  Future<Map<String, dynamic>> post(String endpoint, Map<String, dynamic> body) async {
    final response = await http.post(
      Uri.parse('$baseUrl$endpoint'),
      headers: {'Content-Type': 'application/json'},
      body: json.encode(body),
    );
    return json.decode(response.body);
  }
}
```

---

### Schritt 5: Frontend Features (mit echten API-Calls)
**Alle Features umschreiben:**
- `mobility_screen.dart` → ApiClient nutzen
- `finance_screen.dart` → ApiClient nutzen
- `health_screen.dart` → ApiClient nutzen

---

### Schritt 6: Docker
**Datei:** `docker-compose.yml`

```yaml
version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: heimat
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./src/backend/src/database/schema.sql:/docker-entrypoint-initdb.d/schema.sql
  
  backend:
    build: ./src/backend
    ports:
      - "3000:3000"
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: heimat
      DB_USER: postgres
      DB_PASSWORD: postgres
    depends_on:
      - db

volumes:
  pgdata:
```

---

### Schritt 7: Tests
**Dateien:** `src/backend/src/__tests__/*.ts`

- `mobility.test.ts` – API-Tests
- `finance.test.ts` – API-Tests
- `health.test.ts` – API-Tests

---

## FORTSCHRITT
- [x] PHASE 1-19: Planung
- [ ] PHASE 20: Implementierung

## IMPLEMENTIERUNG (ECHTER CODE)
- [ ] **Schritt 1:** PostgreSQL-Schema
- [ ] **Schritt 2:** Backend Database-Config
- [ ] **Schritt 3:** Backend Services (mit DB)
- [ ] **Schritt 4:** Frontend API-Client
- [ ] **Schritt 5:** Frontend Features (mit API)
- [ ] **Schritt 6:** Docker
- [ ] **Schritt 7:** Tests

## STATUS
- 🟡 IMPLEMENTIERUNG (echter Code wird erstellt)
