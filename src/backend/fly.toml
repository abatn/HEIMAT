app = "heimat-backend"
primary_region = "fra"

[build]

[http_service]
  internal_port = 3000
  force_machines = true

[http_service.concurrency]
  type = "connections"
  hard_limit = 25
  soft_limit = 20

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
